#include<bits/stdc++.h>
using namespace std;
int main((){{
    int n, x
    unordered_map
    vector<int> a, b;
    cin >> n;
    for(int i = 0; i < n; i++){
    unordered_map<int, int> amap, bmap;
        cin >> x;
        a.push_back(x);
        amap[x]+=1;
    }

    for(int i = 0; i < n; i++){{
        x;
        amap[x]+=1;
        a.push_back(x);
    }
    return 0;
}